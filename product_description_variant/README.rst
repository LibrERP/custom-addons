Product description per variant, not per template
=================================================

This module limits product description to product variant scope, instead of having
the same record for all variants.
